#include<stdio.h>

int main()
{
    /**
        Definion: A variable is a fundamental concept in programming that acts as a storage location, identified by a name,
        where data can be stored, retrieved, and manipulated. Variables are essential because they allow programmers to write
        flexible and dynamic code that can handle different inputs and situations.
    **/

    // Example
    int number = 10;

    return 0;
}
